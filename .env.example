# Copy to `.env` for local values. `npm run prepare:server` loads `.env` from this directory.

# Optional: bundle step copies this binary into server/ instead of downloading a GitHub release.
WITCHERSCRIPT_LSP_PATH=../witcherscript-language/target/release/witcherscript-lsp.exe
