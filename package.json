{
  "name": "witcherscript-language-features",
  "displayName": "WitcherScript Language Features",
  "description": "Minimal WitcherScript diagnostics, navigation, and hover support.",
  "version": "0.3.0",
  "publisher": "webspam",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/webspam/witcherscript-vscode.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "main": "./extension.js",
  "scripts": {
    "check": "node --check extension.js && node --check scripts/prepare-server.js",
    "prepare:server": "node scripts/prepare-server.js",
    "package": "npm run prepare:server && vsce package"
  },
  "contributes": {
    "languages": [
      {
        "id": "witcherscript",
        "aliases": [
          "WitcherScript",
          "Witcher Script"
        ],
        "extensions": [
          ".ws"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "witcherscript",
        "scopeName": "source.witcherscript",
        "path": "./syntaxes/witcherscript.tmLanguage.json"
      }
    ],
    "walkthroughs": [
      {
        "id": "witcherscript.gettingStarted",
        "title": "Get Started with WitcherScript",
        "description": "Set up language features for Witcher 3 modding.",
        "steps": [
          {
            "id": "setGameDirectory",
            "title": "Set your Witcher 3 game directory",
            "description": "The language server needs your Witcher 3 install to resolve base game scripts. On Windows with a GOG install this is detected automatically — if not, use the button below.\n\n[Select Game Folder](command:witcherscript.setGameDirectory)",
            "media": {
              "markdown": "walkthrough/game-directory.md"
            },
            "completionEvents": [
              "onSettingChanged:witcherscript.gameDirectory"
            ]
          },
          {
            "id": "openScript",
            "title": "Open a WitcherScript file",
            "description": "Open any `.ws` file to get diagnostics, hover documentation, Go to Definition, and document symbols.",
            "media": {
              "markdown": "walkthrough/open-script.md"
            },
            "completionEvents": [
              "onLanguage:witcherscript"
            ]
          }
        ]
      }
    ],
    "configuration": {
      "title": "WitcherScript",
      "properties": {
        "witcherscript.server.path": {
          "type": "string",
          "default": "",
          "description": "Optional path to the witcherscript-lsp executable. If empty, the extension uses the bundled server binary."
        },
        "witcherscript.gameDirectory": {
          "type": "string",
          "default": "",
          "description": "Absolute path to The Witcher 3 game directory (e.g. C:\\games\\The Witcher 3 Wild Hunt GOTY). Used by the language server to locate base game scripts."
        },
        "witcherscript.additionalScriptDirectories": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Extra root directories to walk recursively for .ws files. Each entry is loaded as read-only base scripts: symbols join the global namespace, but renames are rejected. Use this for co-dependent mods that need to see each other's declarations."
        },
        "witcherscript.autoLoadModSharedImports": {
          "type": "boolean",
          "default": true,
          "description": "Auto-load the Shared Imports mod from <gameDirectory>\\Mods\\modSharedImports if present. Most modern Witcher 3 mods depend on it. Disable to opt out."
        },
        "witcherscript.logLevel": {
          "type": "string",
          "default": "warn",
          "enum": [
            "error",
            "warn",
            "info",
            "debug",
            "trace"
          ],
          "description": "Log verbosity for the WitcherScript language server. Messages appear in the WitcherScript output channel."
        },
        "witcherscript.formatter.lineLimit": {
          "type": "number",
          "default": 100,
          "description": "Maximum line length the formatter will target before wrapping."
        },
        "witcherscript.formatter.compactColon": {
          "type": "boolean",
          "default": false,
          "description": "When enabled, the formatter omits spaces around the colon in type annotations (e.g. `var x:int` instead of `var x : int`)."
        },
        "witcherscript.formatter.alignMemberColons": {
          "type": "boolean",
          "default": false,
          "description": "When enabled, the formatter vertically aligns the colons of consecutive class, struct, and state field declarations into a column. A blank line, comment, or non-field member breaks a group."
        },
        "witcherscript.server.tcpPort": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "maximum": 65535,
          "markdownDescription": "**LSP Testing:** Connect to an externally running language server on this localhost TCP port instead of launching the bundled binary. Leave at `0` to use the bundled server."
        }
      }
    }
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.7.0"
  }
}
